apiVersion: v1
kind: Service
metadata:
  name: smashshop-frontend-service
  namespace: application-deployment
spec:
  type: NodePort
  selector:
    app: smashshop-frontend
  ports:
  - name: https  
    protocol: TCP
    port: 443
    targetPort: 443
    nodePort: 30443
